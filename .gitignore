# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

node_modules/
dist/
coverage/
playwright-report/
test-results/
.wrangler/
.dev.vars
.env
.env.*
!.env.example
*.local
